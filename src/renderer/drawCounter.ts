/**
 * Counter'lar kaldırıldı — servis penceresi kullanılıyor.
 * Bu fonksiyon artık hiçbir şey çizmez, geriye uyum için bırakıldı.
 */
export function drawCounters(
    _ctx: CanvasRenderingContext2D,
    _holdingStations: Array<{ id: string; items: string[]; type: 'plate' | 'counter'; maxItems: number }>
) {
    // Counter'lar kaldırıldı — servis penceresi kullanılıyor
}
